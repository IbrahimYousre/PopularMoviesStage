<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="4dp"
    tools:showIn="@layout/partial_detail_extra">

    <ImageView
        android:id="@+id/thumbnailImageView"
        android:layout_width="200dp"
        android:layout_height="150dp"
        android:adjustViewBounds="true"
        android:contentDescription="@string/trailer_thumbnail_content_description"
        android:scaleType="fitXY"
        tools:src="@sample/backdrops" />

    <ImageView
        android:layout_width="200dp"
        android:layout_height="150dp"
        android:background="@color/semiTransparentScrim"
        android:contentDescription="@string/trailer_thumbnail_content_description" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:contentDescription="@string/trailer_thumbnail_content_description"
        android:tint="@color/primary_light"
        app:srcCompat="@drawable/ic_play_circle_outline_black_32dp" />
</FrameLayout>