<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <padding
        android:bottom="@dimen/round_stroke_vertical_padding"
        android:left="@dimen/round_stroke_horizontal_padding"
        android:right="@dimen/round_stroke_horizontal_padding"
        android:top="@dimen/round_stroke_vertical_padding" />
    <corners android:radius="200dp" />
    <stroke
        android:width="2dp"
        android:color="@color/primary_light" />
    <solid android:color="@color/primary_dark" />
</shape>